apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: nginx-deploy-dev
  name: nginx-deploy-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-deploy-dev
  strategy: {}
  template:
    metadata:
      labels:
        app: nginx-deploy-dev
    spec:
      containers:
      - image: nginx
        name: nginx