apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: nginx-deploy
  name: nginx-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      lob: none
      env: none
  strategy: {}
  template:
    metadata:
      labels:
        lob: none
        env: none
    spec:
      containers:
      - image: nginx
        name: nginx