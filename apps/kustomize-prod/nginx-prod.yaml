apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-prod
      lob: auto
  template:
    metadata:
      labels:
        app: nginx-prod
        lob: auto